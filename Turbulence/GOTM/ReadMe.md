# GOTM-lake branch

用于湖泊模拟的1D Lake模型, 后期已经合并到GOTM代码库

本项目使用的是GOTM的lake分支

git clone --branch lake --recursive https://hub.fgit.cf/gotm-model/code

git submodule update --init --recursive

## GOTM-lake-R

R脚本工具, 如：GOTMr, gotmtools
